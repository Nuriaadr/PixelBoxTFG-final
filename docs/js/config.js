const API_URL =
  window.location.hostname === "127.0.0.1" ||
  window.location.hostname === "localhost"
    ? "http://localhost/PixelBoxTFGPruebas/backend/public"
    : "la url de tu back en produccion to wapo";
